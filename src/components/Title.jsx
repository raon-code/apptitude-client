export default function Title({ title }) {
  return (
    <div className='text-[#FEFEFE] flex items-center justify-center text-[20px] leading-[26px] opacity-[0.98] mt-[8px]'>
      <span>{title}</span>
    </div>
  );
}
